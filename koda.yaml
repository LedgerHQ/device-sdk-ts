# snapshots are not yet registered in the instance
# keeping this commented out for future reference
#snapshot: koda-node20
agentFilesDir: agent-files
setup: corepack enable && pnpm install
lint: pnpm lint
test: pnpm test
