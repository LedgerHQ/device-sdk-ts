---
"@ledgerhq/device-management-kit": minor
---

Export `ConnectToSecureChannelTask` and `SecureChannelEventType` so that consumers can drive a secure channel operation from their own device actions
