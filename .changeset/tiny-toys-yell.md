---
"@ledgerhq/dmk-ledger-wallet": minor
---

Add `CleanDeviceDeviceAction`, uninstalling all installed apps, deleting the installed language pack, and removing the custom lock screen in a single flow.
