---
"@ledgerhq/context-module": minor
---

Surface the Solana CAL instruction-descriptor fields the requirement builder was dropping: `hide_rules` and `owner_association`, plus `TOKEN_VALUE.FALLBACK_ACCOUNT` and `VALUE_FLOW_PORT.ACTIVE_WHEN` on the port shapes.
