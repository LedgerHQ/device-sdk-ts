---
"@ledgerhq/device-management-kit": patch
---

Add the `STRUCT_TYPE_EDIT_LEDGER_ACCOUNT` (`0x30`) address-book TLV constant and the matching `ResponseType.EditLedgerAccount` enum member, used by the signer's new Edit Ledger Account operation.
