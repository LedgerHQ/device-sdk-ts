---
"@ledgerhq/context-module": minor
---

Align Solana CAL instruction descriptors with the `solana_programs` spec and fetch the CALLDATA certificate for the instruction-info and enum-variant loaders
