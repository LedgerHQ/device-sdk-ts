"use client";
import React from "react";

import { SigningView } from "@/components/ContactsView/SigningView";

const Signing: React.FC = () => {
  return <SigningView />;
};

export default Signing;
